name: FC Online 커뮤니티 크롤링

on:
  # 수동 실행
  workflow_dispatch:
    inputs:
      max_pages:
        description: '최대 페이지 수 (0 = 무제한)'
        required: false
        default: '0'
        type: string

# 동시 실행 방지
concurrency:
  group: community-crawler
  cancel-in-progress: false

jobs:
  crawl:
    runs-on: ubuntu-latest
    timeout-minutes: 360  # 최대 6시간

    # 한국 시간대 설정
    env:
      TZ: Asia/Seoul

    steps:
      # 1. 저장소 체크아웃 (github-actions-community 브랜치)
      - name: 📥 저장소 체크아웃
        uses: actions/checkout@v4
        with:
          ref: github-actions-community
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0  # 전체 히스토리 (rebase 위해)

      # 2. Node.js 설정
      - name: 🟢 Node.js 설정
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/crawler-fconline-community/package-lock.json

      # 3. 의존성 설치
      - name: 📦 의존성 설치
        working-directory: backend/crawler-fconline-community
        run: npm ci

      # 4. Puppeteer용 브라우저 설치
      - name: 🌐 Chromium 브라우저 설치
        working-directory: backend/crawler-fconline-community
        run: npx puppeteer browsers install chrome

      # 5. 데이터 폴더 생성
      - name: 📁 데이터 폴더 준비
        working-directory: backend/crawler-fconline-community
        run: mkdir -p data

      # 6. 크롤링 실행
      - name: 🕷️ 커뮤니티 크롤링 실행
        working-directory: backend/crawler-fconline-community
        run: |
          echo "🕐 시작 시간 (KST): $(date '+%Y-%m-%d %H:%M:%S')"
          node ./src/crawler-community.js
          echo "🕐 종료 시간 (KST): $(date '+%Y-%m-%d %H:%M:%S')"
        env:
          MAX_PAGES: ${{ github.event.inputs.max_pages }}

      # 7. 수집 결과 요약
      - name: 📊 수집 결과 요약
        working-directory: backend/crawler-fconline-community
        run: |
          echo "=== 수집 결과 (KST: $(date '+%Y-%m-%d %H:%M:%S')) ==="
          
          # visited.json 확인
          if [ -f data/visited.json ]; then
            VISITED_COUNT=$(cat data/visited.json | jq '.visited | length')
            echo "📋 총 방문 게시글: ${VISITED_COUNT}개"
          fi
          
          # 오늘 날짜 폴더 확인
          TODAY=$(date '+%y-%m-%d')
          if [ -d "data/${TODAY}" ]; then
            if [ -f "data/${TODAY}/posts.jsonl" ]; then
              POST_COUNT=$(wc -l < "data/${TODAY}/posts.jsonl" | tr -d ' ')
              echo "📝 오늘 수집된 게시글: ${POST_COUNT}개"
              echo ""
              echo "최근 5개 게시글:"
              tail -5 "data/${TODAY}/posts.jsonl" | jq -r '.title // "제목없음"' 2>/dev/null || echo "(파싱 오류)"
            fi
          fi

      # 8. 변경사항 커밋 및 푸시 (충돌 방지)
      - name: 💾 수집 데이터 저장
        working-directory: backend/crawler-fconline-community
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # 변경사항 확인
          git add data/
          
          if git diff --staged --quiet; then
            echo "📭 새로운 데이터가 없습니다."
            exit 0
          fi
          
          COMMIT_TIME=$(TZ=Asia/Seoul date '+%Y-%m-%d %H:%M:%S KST')
          git commit -m "📊 커뮤니티 크롤링: ${COMMIT_TIME}"
          
          # 푸시 전 최신 상태 pull (충돌 방지)
          # visited.json 충돌 시 양쪽 모두 병합
          git pull --rebase origin github-actions-community || {
            echo "⚠️ Rebase 충돌 발생. visited.json 병합 시도..."
            
            # visited.json 충돌 해결: 양쪽 visited 배열 병합
            if [ -f data/visited.json ]; then
              # 현재 버전과 원격 버전 병합
              git checkout --theirs data/visited.json 2>/dev/null || true
              git add data/visited.json
              git rebase --continue || git rebase --abort
            fi
          }
          
          git push origin github-actions-community
          echo "✅ github-actions-community 브랜치에 저장 완료"

      # 9. 아티팩트 업로드 (백업용)
      - name: 📤 데이터 아티팩트 업로드
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: crawl-data-${{ github.run_id }}
          path: backend/crawler-fconline-community/data/
          retention-days: 30
